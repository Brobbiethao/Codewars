function GetSum( a,b ) {
  var total = 0;
  if(a === b) {
    return a;
  } else if( a < b) {
    for(var i = a; i <= b; i++) {
      total+=i;
    }
  } else if ( a > b) {
    for(var i = b; i <= a; i++) {
      total+=i;
    }
  }
  return total;
}
